# n8n-clickup-in-testing-tracker
n8n workflow that tracks tasks moved to QA/Testing statuses ("qa review", "client review") in ClickUp → accumulates them per team member per day in Google Sheets (prevents duplicates). Part of multi-team daily task logging system (variants for pending, in-testing, completed, etc. — already done for devops, backend, frontend, QA, AI).
